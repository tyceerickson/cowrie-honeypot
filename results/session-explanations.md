# Session Explanations — Ollama LLM Analysis

> Generated: 2026-05-28 17:08 UTC  
> Model: llama3.1:8b (local, Alienware RTX 4070)  
> Input: Cowrie honeypot session data  
> Script: `analysis/explain_sessions.py`

Plain-English explanations of representative attack sessions,
generated by a local LLM. Mirrors the format of Project 2's
`results/explanations.md` (ai-traffic-classifier).

---

## Web Attack (nginx)

**Attack type:** Automated scanner or exploit tool probing the web server for known vulnerabilities, exposed configuration files, or exploitable endpoints.

### Session #1

**Session Metadata:**

| Field | Value |
|-------|-------|
| Source IP | `45.139.122.80` |
| Country | unknown |
| Organization | unknown |
| ASN | `unknown` |
| SSH Client | `unknown` |
| HASSH | `unknown` |
| Duration | N/As |
| Timestamp | unknown |

**Session Transcript:**

```
GET /cgi-bin/luci/;stok=/locale HTTP/1.1
```

**Analysis:**

Here's the analysis:

**1. Attack Type:** This request is probing for a potential LFI (Local File Inclusion) vulnerability, attempting to access sensitive files on the server.

**2. Tool/Scanner:** The tool likely generating this request is OpenVAS or another Nmap-based scanner, which are commonly used for vulnerability scanning and exploitation attempts.

**3. CVE or Technique:** This attack technique is related to a known LFI vulnerability, but it's not targeting a specific CVE ID. However, it's similar to the LFI attacks described in CVE-2019-6340 (OpenWRT).

**4. MITRE ATT&CK:** This request maps to the T1055: Web Server Attacks technique (ID 1055), which involves exploiting vulnerabilities on web servers.

**5. Defender Action:** To prevent this, the defender should implement a WAF (Web Application Firewall) rule blocking requests with suspicious query parameters, such as `;stok=/locale`, and ensure that all CGI scripts are properly configured to prevent LFI attacks.

---

### Session #2

**Session Metadata:**

| Field | Value |
|-------|-------|
| Source IP | `77.68.25.99` |
| Country | unknown |
| Organization | unknown |
| ASN | `unknown` |
| SSH Client | `unknown` |
| HASSH | `unknown` |
| Duration | N/As |
| Timestamp | unknown |

**Session Transcript:**

```
POST /cgi-bin/.%2e/.%2e/.%2e/.%2e/.%2e/.%2e/.%2e/.%2e/.%2e/.%2e/bin/sh HTTP/1.1
```

**Analysis:**

**Analysis**

**1. Attack Type:** This request is probing for a CGI (Common Gateway Interface) vulnerability, specifically attempting to access the `/bin/sh` shell through multiple levels of URL encoding.

**2. Tool/Scanner:** The tool likely generating this request is an automated web scanner or a penetration testing tool, possibly using a vulnerability scanning framework like Nmap or Burp Suite.

**3. CVE or Technique:** This attack pattern maps to the "Path Traversal" technique (T4418) and may be related to the "CVE-2006-3747: Apache mod_cgi CGI-bin directory traversal vulnerability". However, without further context, it's difficult to pinpoint a specific CVE ID.

**4. MITRE ATT&CK:** This attack pattern maps to the "Path Traversal" technique (T4418) in the MITRE ATT&CK framework.

**5. Defender Action:** To prevent this type of attack, implement URL encoding validation and filtering on the web server, specifically blocking requests with multiple levels of URL encoding or suspicious characters like `.` and `/`.

---

### Session #3

**Session Metadata:**

| Field | Value |
|-------|-------|
| Source IP | `77.68.25.99` |
| Country | unknown |
| Organization | unknown |
| ASN | `unknown` |
| SSH Client | `unknown` |
| HASSH | `unknown` |
| Duration | N/As |
| Timestamp | unknown |

**Session Transcript:**

```
POST /cgi-bin/%%32%65%%32%65/%%32%65%%32%65/%%32%65%%32%65/%%32%65%%32%65/%%32%65%%32%65/%%32%65%%32%65/%%32%65%%32%65/bin/sh HTTP/1.1
```

**Analysis:**

Here is the analysis:

**1. Attack Type:** This request appears to be a CGI (Common Gateway Interface) command injection attack, attempting to execute arbitrary shell commands on the web server.

**2. Tool/Scanner:** The URL encoding pattern suggests that this request was likely generated by a tool such as Burp Suite or ZAP, possibly with some manual modification to evade detection.

**3. CVE or Technique:** This is not targeting a specific known CVE, but it is related to the technique of "Command and Control" (T1055) in the MITRE ATT&CK framework.

**4. MITRE ATT&CK:** This request maps to the "Command and Control" technique (T1055), specifically attempting to execute shell commands on the web server.

**5. Defender Action:** To prevent this, I would recommend disabling CGI execution for all directories except those that require it, as well as implementing URL encoding normalization to detect and block similar attacks.

---

