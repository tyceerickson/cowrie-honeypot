# Session Explanations — Ollama LLM Analysis

> Generated: 2026-05-22 16:17 UTC  
> Model: llama3.1:8b (local, Alienware RTX 4070)  
> Input: Cowrie honeypot session data  
> Script: `analysis/explain_sessions.py`

Plain-English explanations of representative attack sessions,
generated by a local LLM. Mirrors the format of Project 2's
`results/explanations.md` (ai-traffic-classifier).

---

## SSH Key Backdoor Implant

**Attack type:** Attacker logs in and immediately attempts to install a persistent SSH backdoor by injecting their public key into `~/.ssh/authorized_keys`. This gives them permanent access even after the password is changed.

### Session #1

**Session Metadata:**

| Field | Value |
|-------|-------|
| Source IP | `114.130.85.36` |
| Country | Bangladesh |
| Organization | Tire-1 IP Transit Provider of Bangladesh |
| ASN | `AS17806` |
| SSH Client | `SSH-2.0-libssh_0.9.6` |
| HASSH | `f555226df1963d1d3c09daf865abdc9a` |
| Duration | 8.8s |
| Timestamp | 2026-05-22T00:16:15.673366Z |

**Session Transcript:**

```
SUCCESSFUL LOGIN: root / Testtest1
COMMAND: cd ~; chattr -ia .ssh; lockr -ia .ssh
COMMAND: cd ~ && rm -rf .ssh && mkdir .ssh && echo "ssh-rsa AAAAB3NzaC1yc2EAAAABJQAAAQEArDp4cun2lhr4KUhBGE7VvAcwdli2a8dbnrTOrbMz1+5O73fcBOx8NVbUT0bUanUV9tJ2/9p7+vD0EpZ3Tz/+0kX34uAx1RV/75GVOmNx+9EuWOnvNoaJe0QXx... [truncated]
```

**Analysis:**

**Attack Behavior**

The attacker successfully logged in as the root user with a weak password ("Testtest1") and immediately executed two commands. The first command attempted to disable SSH access logging by modifying file attributes, while the second command deleted the existing .ssh directory and created a new one, potentially allowing the attacker to establish a persistent backdoor.

**Tool Identification**

The attacker used the libssh_0.9.6 library, which is an open-source implementation of the SSH-2 protocol. The HASSH fingerprint (f555226df1963d1d3c09daf865abdc9a) suggests that this library was used to establish a connection.

**Attacker Intent**

The attacker's primary goal appears to be establishing a persistent backdoor on the compromised system, allowing them to maintain access and potentially escalate privileges. This is achieved by deleting the existing .ssh directory and creating a new one, which can be used to upload malicious files or execute commands remotely.

**Key Indicators**

1. **Successful login with weak password**: The attacker exploited a weak password ("Testtest1") to gain root access.
2. **Modification of file attributes**: The command `chattr -ia .ssh` disabled SSH access logging, indicating an attempt to cover their tracks.
3. **Creation of new .ssh directory**: The attacker created a new .ssh directory, potentially allowing them to establish a persistent backdoor.

**MITRE ATT&CK**

This attack maps to the following techniques:

* T1055: Process Injection (attempting to disable SSH access logging and create a new .ssh directory)
* T1210: Exploitation of Remote Services (using weak password to gain root access via SSH)

---

### Session #2

**Session Metadata:**

| Field | Value |
|-------|-------|
| Source IP | `68.233.116.124` |
| Country | India |
| Organization | Oracle Corporation |
| ASN | `AS31898` |
| SSH Client | `SSH-2.0-libssh_0.9.6` |
| HASSH | `671ac49b8bd65b9e8ff02a3e690f0fd3` |
| Duration | 6.2s |
| Timestamp | 2026-05-22T00:24:13.860785Z |

**Session Transcript:**

```
SUCCESSFUL LOGIN: root / q123456
COMMAND: cd ~; chattr -ia .ssh; lockr -ia .ssh
COMMAND: cd ~ && rm -rf .ssh && mkdir .ssh && echo "ssh-rsa AAAAB3NzaC1yc2EAAAABJQAAAQEArDp4cun2lhr4KUhBGE7VvAcwdli2a8dbnrTOrbMz1+5O73fcBOx8NVbUT0bUanUV9tJ2/9p7+vD0EpZ3Tz/+0kX34uAx1RV/75GVOmNx+9EuWOnvNoaJe0QXx... [truncated]
```

**Analysis:**

**Attack Session Analysis**

**1. Attack Behavior**
The attacker successfully logged in as the root user with a weak password 'q123456' and immediately executed two commands: changing file attributes of the .ssh directory, and then deleting it while creating a new one with the same name. This behavior is indicative of an attempt to erase existing SSH keys or configurations.

**2. Tool Identification**
The attacker used the libssh_0.9.6 client, which is an open-source implementation of the SSH protocol. The use of this specific client version and its corresponding HASSH fingerprint (671ac49b8bd65b9e8ff02a3e690f0fd3) suggests that the attacker may be using a known exploit or tool.

**3. Attacker Intent**
The attacker's intent is likely to erase existing SSH keys or configurations, potentially allowing them to establish a new SSH connection without being detected by intrusion detection systems (IDS). This behavior is characteristic of an implant attack, where the attacker attempts to gain persistent access to the system.

**4. Key Indicators**

* Successful login as root with weak password 'q123456'
* Immediate execution of commands modifying .ssh directory attributes and contents
* Use of libssh_0.9.6 client with specific HASSH fingerprint

**5. MITRE ATT&CK**
This attack session maps to the following techniques:

* T1210: Exploit Public-Facing Application (specifically, SSH protocol)
* T1053: File and Directory Discovery
* T1140: Deception Techniques

---

## System Reconnaissance

**Attack type:** Attacker logs in and runs system enumeration commands to assess the target before deciding on next steps. Classic pre-exploitation intelligence gathering.

### Session #1

**Session Metadata:**

| Field | Value |
|-------|-------|
| Source IP | `176.65.139.99` |
| Country | Luxembourg |
| Organization | Offshore LC |
| ASN | `AS214472` |
| SSH Client | `unknown` |
| HASSH | `unknown` |
| Duration | 4.352063894271851s |
| Timestamp | 2026-05-22T10:33:26.326664Z |

**Session Transcript:**

```
SUCCESSFUL LOGIN: root / 
COMMAND: echo SHELL_TEST
COMMAND: /bin/busybox TEST
COMMAND: cat /proc
COMMAND: ./
```

**Analysis:**

**Attack Session Analysis**

**1. Attack Behavior:**
The attacker attempted a reconnaissance attack, attempting to gather information about the system by executing various commands. They successfully logged in as the root user and executed several commands, including checking for BusyBox, listing processes, and attempting to access sensitive directories.

**2. Tool Identification:**
Based on the evidence, it appears that the attacker used a generic SSH client or a tool that mimics an SSH client, as no specific SSH client is identified in the session transcript. The use of telnet protocol suggests that the attacker may be using a tool that can switch between protocols.

**3. Attacker Intent:**
The attacker's intent was to gather information about the system and its configuration, likely for future exploitation or reconnaissance purposes. They attempted to identify potential vulnerabilities by checking for BusyBox and listing processes.

**4. Key Indicators:**

* Successful login as root user
* Execution of suspicious commands (e.g., cat /proc)
* Use of telnet protocol

**5. MITRE ATT&CK:**
This attack maps to the following techniques:

* T1059: Command and Control (C2) - Reconnaissance (attempting to gather information about the system)
* T1041: Network Device Misconfiguration - Information Gathering (executing commands to identify potential vulnerabilities)

Note: The use of telnet protocol is not a specific technique in MITRE ATT&CK, but it may indicate that the attacker is using a tool or method that can switch between protocols.

---

